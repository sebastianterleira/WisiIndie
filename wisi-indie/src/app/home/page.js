'use client';
import styled from 'styled-components';

const Title = styled.h1`
	color: var(--textRainbow);
`;

const Home = () => {
	return (
    <main>
      <Title>HelloWorld</Title>
    </main>
  )
};

export default Home;
